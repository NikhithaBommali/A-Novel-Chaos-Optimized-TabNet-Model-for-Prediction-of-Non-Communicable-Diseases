{
  "backend": {
    "runtime": "python",
    "port": 3000,
    "install": "cd API && pip install --no-cache-dir -r requirements.txt",
    "start": "cd API && uvicorn main:app --host 0.0.0.0 --port 3000",
    "files": ["API/**"]
  }
}
