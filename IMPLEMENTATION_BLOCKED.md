# Implementation blocked

I could not inspect or modify the requested Next.js UI because the workspace provided to the task was empty.

Checks performed:
- glob `UI/**/*` -> no matches
- glob `**/*` -> no matches

Why no code changes were made:
- The task requires preserving and minimally fixing the existing checked-in UI under `UI/`.
- With no repository files present, creating a replacement app from scratch would not be a faithful preview of the existing implementation and would violate the task constraints.

What is needed:
- The repository contents, especially `UI/package.json`, `UI/app/`, `UI/components/`, `UI/lib/`, `UI/hooks/`, `UI/next.config.mjs`, and `UI/tsconfig.json`.
